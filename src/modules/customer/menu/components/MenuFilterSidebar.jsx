import { RotateCcw, Search, SlidersHorizontal, Tag } from "lucide-react";

export default function MenuFilterSidebar({ categories = [], activeCategory, onSelectCategory, searchQuery = "", onSearchChange, maxPrice = 100, onChangeMaxPrice, onResetFilters, isMobileDrawer = false, onCloseMobileDrawer }) {
  return <aside className={`menu-filter-sidebar-card ${isMobileDrawer ? "mobile-drawer-view" : ""}`}><div className="filter-sidebar-header"><div className="filter-title-wrapper"><h3 className="filter-main-title"><SlidersHorizontal size={17}/> فلترة المنتجات</h3><div className="filter-title-underline"/></div>{isMobileDrawer && <button className="filter-close-btn" onClick={onCloseMobileDrawer}>×</button>}</div><label className="table-menu-filter-field"><span><Search size={15}/>البحث بالاسم</span><input value={searchQuery} onChange={(e) => onSearchChange(e.target.value)} placeholder="اسم المنتج"/></label><label className="table-menu-filter-field"><span>القسم</span><select value={activeCategory} onChange={(e) => onSelectCategory(e.target.value)}>{categories.map((category) => <option key={category.id} value={category.id}>{category.title}</option>)}</select></label><div className="filter-section-group"><div className="filter-section-toggle"><div className="section-label-group"><Tag size={17}/><span className="filter-section-name">السعر حتى {maxPrice} EGP</span></div></div><div className="price-slider-container"><input type="range" min="10" max="150" step="5" value={maxPrice} onChange={(e) => onChangeMaxPrice(Number(e.target.value))} className="custom-range-slider"/><div className="price-labels-row"><span>10 EGP</span><span>{maxPrice} EGP</span></div></div></div><button type="button" className="reset-filters-btn" onClick={onResetFilters}><span>مسح الفلاتر</span><RotateCcw size={15}/></button></aside>;
}
