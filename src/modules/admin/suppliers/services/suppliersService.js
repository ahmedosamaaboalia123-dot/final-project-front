// Compatibility exports for imports removed as supplier screens migrate.
// getSupplierOptions stays until phase 5 migrates the inventory screens.
export { getSupplierOptions, suppliersApi as default, suppliersApi } from "../api/suppliers.api";
