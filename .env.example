# Frontend API base. Both forms are supported during migration; phase 1 normalizes to /api/v1.
VITE_API_BASE_URL=http://localhost:5000/api

# Socket server origin, without /api.
VITE_SOCKET_URL=http://localhost:5000

