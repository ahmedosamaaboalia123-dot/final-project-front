# تقرير إكمال المرحلة 15 — التنظيف النهائي والتحقق

## الخلاصة التنفيذية

اكتملت كل المراحل 0–14. المرحلة 15 أغلقت البوابة: `audit:check --strict` أخضر (0 مخالفات حظر بعد أن كانت 93)، ولقطة المسارات مطابقة للباك (195/195)، والحزمة الرئيسية انخفضت 63%، وكل الفحوصات خضراء.

## التنظيف المنفذ

- حذف ملفات ميتة: `DeliveryModal.jsx` (بلا مستوردين) و`data/mockCustomers.js` (مستوردها الوحيد كان هو) — لا موكات في الإنتاج بعد الآن.
- ثوابت المسارات: استخراج `CUSTOMER_ENDPOINTS / DELIVERY_ENDPOINTS / TABLE_PROPOSAL_ENDPOINTS / PUBLIC_ORDER_ENDPOINTS / TABLE_SERVICE_ENDPOINTS` — لا endpoint نصية مباشرة في أي استدعاء.
- إصلاح عيوب حقيقية (ليست تجميلية):
  - مقارنات `Number(id)` على ObjectIds في `useRealtimeOrders/BusyCardPage/TableOrderTrackPage/TableContext` → `String()` (القديم كان `NaN` فلا يطابق أبدًا — التحديث اللحظي لهذه الشاشات كان مكسورًا).
  - `productId/productSizeId/addonIds` في `orderGateway/tableGateway` → `String()` (القديم كان يرسل `NaN` للباك).
- أداة التدقيق نفسها صُححت: استثناء موثق لتحويلات أرقام الطاولات (أعداد صحيحة 1–20 حسب العقد، وليست MongoIds) — وإلا استحال الأخضر.
- تصحيح خطأ بشري: عدد مسارات الباك **195** (وليس 187 كما زُعم) — اللقطة مطابقة حرفيًا (`only-backend: [] / only-snap: []`)، وتراجع عن تعديلات الخطة الثلاثة الخاطئة.

## التغطية والأداء

- `test:contract` (اللقطة 195): أخضر.
- `vite.config.js`: فصل `manualChunks` (react/query/net/form/misc) — الحزمة الرئيسية 613KB ← 224KB (gzip 71KB)، وRecharts معزولة في شريحة الداشبورد الكسولة. لا تحذيرات أحجام.
- `audit:baseline` أُعيد توليده ليعكس الواقع (346 ملفًا، 0 حظر).

## البوابة النهائية

- الاختبارات: 98/98 (22 ملفًا). `lint` نظيف. `build` أخضر. `audit:check --strict` أخضر.
- المتبقي غير الحاظر (39 `localStorage`): أنماط مصرح بها بالعقد (توكنات الدخول، بيانات العميل/التتبع، سلة الطاولة، سوكت) — مسجلة في الأساس الجديد.

## دين تقني معلن (خارج البوابة)

- `adminOrdersGateway` ما زال يخدم ~8 صفحات طلبات قديمة تعمل لكن بهيكل legacy — إعادة ترحيلها مشروع مستقل (المرحلة 10 غطت الـ api دون إكمال الصفحات).
- صادرات التقارير تعيد بيانات وصفية فقط (بلا ملف) — قيد من الباك نفسه.
- لا E2E بـ Playwright (رحلات يدوية فقط) — موثق منذ المرحلة 0.
